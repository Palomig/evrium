# 🚀 Автоматический деплой на Timeweb настроен!

Я настроил автоматическую сборку и деплой через GitHub Actions.

## ✅ Что сделано:

1. **Создан workflow** `.github/workflows/deploy-timeweb.yml`
2. Workflow будет:
   - Автоматически собирать проект (`npm run build`)
   - Загружать только папку `dist` на Timeweb через FTP
   - Запускаться при каждом пуше в `main` ветку

## 🔧 Что нужно сделать ВАМ:

### Шаг 1: Получите FTP данные от Timeweb

1. Войдите в панель управления Timeweb
2. Перейдите: **Хостинг → FTP-доступ**
3. Найдите или создайте FTP аккаунт и запишите:
   - **Сервер** (например: `ftp.ваш-домен.ru`)
   - **Логин**
   - **Пароль**

### Шаг 2: Добавьте FTP данные в GitHub Secrets

1. Откройте репозиторий: https://github.com/Palomig/evrium

2. Перейдите: **Settings → Secrets and variables → Actions**

3. Нажмите **"New repository secret"** и добавьте **3 секрета**:

   **Секрет 1:**
   ```
   Name: FTP_SERVER
   Secret: ftp.ваш-домен.ru
   ```

   **Секрет 2:**
   ```
   Name: FTP_USERNAME
   Secret: ваш_ftp_логин
   ```

   **Секрет 3:**
   ```
   Name: FTP_PASSWORD
   Secret: ваш_ftp_пароль
   ```

### Шаг 3: Смёржите эту ветку в main

```bash
# Создайте Pull Request или смёржите напрямую:
git checkout main
git merge claude/mobile-responsive-docs-site-011CUmyym9Hd4GGrQxCmnfWM
git push
```

## 🎯 Как это работает:

После настройки секретов:

```
Вы делаете git push → GitHub собирает проект → Загружает dist/ на Timeweb → Сайт обновлён!
```

Всё происходит автоматически за 2-3 минуты.

## 📊 Мониторинг деплоя

Смотрите статус:
1. Откройте: https://github.com/Palomig/evrium/actions
2. Выберите последний workflow **"Build and Deploy to Timeweb"**
3. Смотрите логи

## ⚠️ Текущая проблема

Сейчас на вашем Timeweb хостинге лежат **исходники** (src/, package.json), а должны лежать **собранные файлы** (из dist/).

### Решение:

**Вариант 1 (рекомендуется):**
После настройки GitHub Actions:
1. Очистите папку `public_html` на Timeweb
2. Сделайте `git push` в main
3. GitHub Actions автоматически загрузит правильные файлы

**Вариант 2 (быстрый):**
1. Соберите проект локально: `npm run build`
2. Вручную загрузите содержимое `dist/` и `.htaccess` в `public_html`

## 🔍 Детальная инструкция

Смотрите файл: `.github/SECRETS_SETUP.md`

## ❓ Вопросы?

- Где найти FTP данные? → В письме от Timeweb или в панели управления
- Не работает? → Проверьте логи в Actions на GitHub
- Ошибка 500? → Проверьте, что `.htaccess` загружен

---

**После настройки** просто делайте `git push` - сайт будет обновляться автоматически! 🎉
