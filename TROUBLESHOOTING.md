# 🔧 Быстрое исправление деплоя на Timeweb

## Проблема
На хостинг загрузились исходники (src/), но shared хостинг не может их запускать.
Нужны собранные файлы из папки `dist`.

## ✅ Решение 1: Ручная загрузка (работает сразу)

### Шаг 1: Соберите проект локально
```bash
cd /path/to/evrium
npm install
npm run build
```

После выполнения появится папка `dist/` с готовыми файлами.

### Шаг 2: Очистите public_html на Timeweb

В файловом менеджере Timeweb:
1. Откройте папку `public_html`
2. **Удалите ВСЁ** (кроме .htaccess если он там есть):
   - src/
   - node_modules/
   - package.json
   - все файлы конфигурации

### Шаг 3: Загрузите содержимое dist/

1. Откройте папку `dist/` на вашем компьютере
2. Выделите **ВСЕ файлы внутри** dist/:
   - index.html
   - assets/
   - vite.svg
3. Загрузите их в `public_html` на Timeweb

### Шаг 4: Загрузите .htaccess

1. Найдите файл `.htaccess` в корне проекта
2. Загрузите его в `public_html`

### Шаг 5: Проверьте структуру

В `public_html` должно быть:
```
public_html/
├── .htaccess
├── index.html
├── assets/
│   ├── index-XXXXX.js
│   ├── index-XXXXX.css
│   └── ...
└── vite.svg
```

**НЕ должно быть:**
- ❌ src/
- ❌ node_modules/
- ❌ package.json
- ❌ vite.config.js

---

## ✅ Решение 2: Автоматический деплой (для будущего)

После настройки GitHub Actions (как я показал ранее):

1. Добавьте FTP секреты в GitHub
2. Смёржите ветку в main
3. При каждом push проект будет собираться и деплоиться автоматически

---

## 🐛 Проверка после загрузки

1. Откройте ваш домен в браузере
2. Нажмите F12 → Console
3. Если есть ошибки - сообщите какие

## ❓ Частые ошибки

### "Вижу список файлов"
→ Нет index.html в корне public_html

### "Ошибка 500"
→ Проблема с .htaccess или правами доступа (должны быть 644)

### "Белая страница, в консоли ошибки 404 на JS/CSS"
→ Неправильные пути к assets, проверьте vite.config.js

### "Страницы не открываются напрямую"
→ Нет .htaccess или mod_rewrite не работает

---

## 📞 Нужна помощь?

Напишите:
1. Что видите на сайте (скриншот)
2. Какие файлы в public_html
3. Ошибки из консоли браузера (F12)
