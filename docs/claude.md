Отлично 💪
Теперь у тебя получается **полноценная CRM-платформа для репетиторского центра**, а не просто личный инструмент — с возможностью добавить нескольких преподавателей и супер-админов, плюс API для интеграций (например, с Telegram-ботом).

Вот **обновлённое и дополненное техническое задание**, полностью учитывающее все пожелания.

---

# 🧾 Техническое задание (v2)

## Проект: CRM-система для репетиторов по математике

### с поддержкой нескольких преподавателей, супер-админов и API

---

## 🔧 1. Общая концепция

Система предназначена для учёта работы с учениками в офлайн-формате.
Каждый **преподаватель** ведёт собственных учеников, фиксирует уроки, оплаты, прогресс и домашние задания.
**Супер-админы** могут видеть всех учеников, отчёты и статистику по всем преподавателям.

Сайт также предоставляет **REST-API** для интеграции с Telegram-ботом или другими внешними сервисами.

---

## ⚙️ 2. Технологический стек

**Frontend:**

* HTML5, CSS3, JavaScript (Vanilla или Alpine.js)
* TailwindCSS — через CDN
* Chart.js — для диаграмм прогресса
* jsPDF — для генерации отчётов

**Backend:**

* PHP 8.x (чистый PHP, без фреймворков)
* MySQL (InnoDB, UTF8MB4)
* REST-API на PHP (`/api/*`)

**Развёртывание:**

* Shared-hosting (Apache или Nginx)
* Без Composer и внешних библиотек
* Авторизация — через PHP-сессии + токены для API

---

## 🧱 3. Архитектура проекта

```
/index.php                 — панель преподавателя
/student.php               — карточка ученика
/report.php                — отчёт по ученику
/login.php                 — страница входа

/admin/
   dashboard.php           — панель супер-админа
   teachers.php            — управление преподавателями
   students.php            — все ученики
   reports.php             — сводные отчёты
   settings.php            — глобальные настройки

/api/
   auth.php                — авторизация и выдача токенов
   students.php            — CRUD по ученикам
   lessons.php             — CRUD по урокам
   skills.php              — CRUD по скиллам
   payments.php            — CRUD по оплатам
   reports.php             — генерация PDF и JSON отчётов
   telegram.php            — эндпоинты для Telegram-бота

/config/
   db.php                  — подключение MySQL
   auth.php                — функции авторизации
   helpers.php             — утилиты

/assets/
   /css/                   — Tailwind (через CDN)
   /js/                    — main.js, api.js, report.js
   /materials/             — PDF/изображения

```

---

## 🧩 4. Структура базы данных

### Таблица `admins` (ранее — преподаватели)

| Поле          | Тип                          | Описание               |
| ------------- | ---------------------------- | ---------------------- |
| id            | INT PK AUTO                  | ID                     |
| username      | VARCHAR(50)                  | Логин                  |
| password_hash | VARCHAR(255)                 | Пароль                 |
| name          | VARCHAR(100)                 | Отображаемое имя       |
| role          | ENUM('superadmin','teacher') | Роль пользователя      |
| active        | BOOLEAN                      | Активен / нет          |
| telegram_id   | BIGINT NULL                  | ID Telegram (для бота) |
| created_at    | DATETIME                     | Дата регистрации       |

---

### Таблица `students`

| Поле       | Тип                                        | Описание        |
| ---------- | ------------------------------------------ | --------------- |
| id         | INT PK AUTO                                | ID              |
| teacher_id | INT FK → admins.id                         | Преподаватель   |
| name       | VARCHAR(100)                               | Имя             |
| class      | INT                                        | Класс           |
| phone      | VARCHAR(20)                                | Телефон         |
| schedule   | VARCHAR(255)                               | Расписание      |
| goal       | VARCHAR(255)                               | Цель            |
| comment    | TEXT                                       | Комментарий     |
| balance    | DECIMAL(8,2)                               | Баланс          |
| status     | ENUM('оплачено','ожидает','задолженность') | Статус          |
| created_at | DATETIME                                   | Дата добавления |

---

### Таблица `skills`

(общие для всех преподавателей)

| Поле        | Тип          | Описание  |
| ----------- | ------------ | --------- |
| id          | INT PK AUTO  | ID        |
| name        | VARCHAR(100) | Название  |
| category    | VARCHAR(100) | Категория |
| class       | INT          | Класс     |
| description | TEXT         | Описание  |

---

### Таблица `student_skills`

| Поле        | Тип                  | Описание            |
| ----------- | -------------------- | ------------------- |
| id          | INT PK AUTO          | ID                  |
| student_id  | INT FK → students.id |                     |
| skill_id    | INT FK → skills.id   |                     |
| level       | TINYINT              | Уровень (0–5)       |
| last_update | DATE                 | Последнее изменение |
| comment     | TEXT                 | Замечания           |

---

### Таблица `lessons`

| Поле           | Тип                  | Описание          |
| -------------- | -------------------- | ----------------- |
| id             | INT PK AUTO          | ID                |
| student_id     | INT FK → students.id |                   |
| teacher_id     | INT FK → admins.id   |                   |
| date           | DATE                 | Дата урока        |
| topics         | TEXT                 | Темы              |
| homework_given | BOOLEAN              | Домашка дана      |
| homework_done  | BOOLEAN              | Домашка выполнена |
| comment        | TEXT                 | Комментарий       |
| paid           | BOOLEAN              | Оплата за урок    |
| rating         | TINYINT              | Оценка (1–5)      |

---

### Таблица `payments`

| Поле       | Тип                  | Описание             |
| ---------- | -------------------- | -------------------- |
| id         | INT PK AUTO          | ID                   |
| student_id | INT FK → students.id |                      |
| teacher_id | INT FK → admins.id   |                      |
| date       | DATE                 | Дата                 |
| amount     | DECIMAL(8,2)         | Сумма                |
| method     | VARCHAR(50)          | Метод (нал, перевод) |
| comment    | TEXT                 | Примечание           |

---

### Таблица `materials`

| Поле        | Тип                 | Описание    |
| ----------- | ------------------- | ----------- |
| id          | INT PK AUTO         | ID          |
| teacher_id  | INT FK → admins.id  |             |
| name        | VARCHAR(100)        | Название    |
| category    | VARCHAR(100)        | Категория   |
| class       | INT                 | Класс       |
| type        | ENUM('pdf','image') | Тип         |
| path        | VARCHAR(255)        | Путь        |
| description | TEXT                | Комментарий |

---

## 🔐 5. Роли и права доступа

| Действие                         | Teacher         | Superadmin  |
| -------------------------------- | --------------- | ----------- |
| Вести своих учеников             | ✅               | ✅           |
| Просматривать чужих учеников     | ❌               | ✅           |
| Добавлять/удалять преподавателей | ❌               | ✅           |
| Управлять скиллами/темами        | ✅               | ✅           |
| Управлять файлами/материалами    | ✅ (только свои) | ✅ (все)     |
| Генерировать отчёты              | ✅ (по своим)    | ✅ (по всем) |
| Просматривать финансы всех       | ❌               | ✅           |

---

## 🧠 6. REST-API (для Telegram и внешних сервисов)

Все запросы работают по токену (Bearer Token), выданному преподавателю или супер-админу.

Базовый URL: `/api/`

| Эндпоинт                                     | Метод | Описание                               |
| -------------------------------------------- | ----- | -------------------------------------- |
| `auth/login`                                 | POST  | Авторизация (возврат токена)           |
| `auth/check`                                 | GET   | Проверка токена                        |
| `students/list`                              | GET   | Список учеников (по преподавателю)     |
| `students/get/{id}`                          | GET   | Данные ученика                         |
| `students/add`                               | POST  | Добавить ученика                       |
| `students/update/{id}`                       | POST  | Изменить ученика                       |
| `lessons/list?student={id}`                  | GET   | Список уроков                          |
| `lessons/add`                                | POST  | Добавить урок                          |
| `payments/add`                               | POST  | Добавить оплату                        |
| `reports/monthly?student={id}&month=YYYY-MM` | GET   | JSON-отчёт                             |
| `reports/pdf?student={id}&month=YYYY-MM`     | GET   | PDF-файл отчёта                        |
| `telegram/link`                              | POST  | Привязка Telegram-пользователя по коду |
| `telegram/progress`                          | GET   | Получить успеваемость по Telegram ID   |
| `skills/list`                                | GET   | Все скиллы по классу/категории         |
| `student_skills/update`                      | POST  | Изменить уровень навыка                |

---

## 🤖 7. Пример сценариев для Telegram-бота

* `/start` — привязка Telegram к ученику (по коду, который задаёт преподаватель).
* `/progress` — получить краткий отчёт (уроки, выполненные домашки, прогресс).
* `/nextlesson` — показать дату следующего занятия.
* `/report` — получить ссылку на последний PDF-отчёт.

---

## 📊 8. Интерфейс и UX

**Панель преподавателя:**

* Список его учеников (фильтр, поиск)
* Кнопка "Добавить урок"
* Кнопка "Отчёт за месяц"
* Отображение баланса и задолженностей

**Панель супер-админа:**

* Переключатель "все преподаватели"
* Сводная статистика:

  * Количество уроков за месяц
  * Средний процент домашних
  * Общая сумма оплат
* Таблицы и графики по преподавателям

---

## 📄 9. Отчёты

**Формат PDF:**

* Имя ученика, месяц, класс
* Таблица уроков
* График (Chart.js → Canvas → PDF)
* Рейтинг выполнения домашних
* Баланс и оплатa
* Комментарий преподавателя

Отчёты можно:

* скачать вручную,
* экспортировать через API (для Telegram),
* формировать автоматически в конце месяца.

---

## 🔐 10. Безопасность и права

* Все запросы к `/api/` требуют токен (`Authorization: Bearer <token>`).
* Авторизация преподавателей через PHP-сессии.
* Пароли — `password_hash()` / `password_verify()`.
* Роли проверяются на каждом запросе.
* CSRF-токены для всех POST-форм.

---

## 🚀 11. Минимальный MVP

1. Авторизация (teacher / superadmin)
2. CRUD учеников, уроков, оплат
3. Учёт домашних заданий
4. Навыки (скиллы) и уровни
5. Генерация PDF-отчётов
6. REST-API для Telegram
7. Админ-панель для супер-админов

---

## 📈 12. Потенциальное развитие

* Поддержка расписания и напоминаний (email / Telegram).
* Возможность учеников оставлять обратную связь.
* Telegram-бот для преподавателей (добавление уроков с телефона).
* Импорт/экспорт учеников в CSV.
* Расширенная аналитика по успеваемости.
