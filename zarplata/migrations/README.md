# Инструкция по применению миграции

## Миграция: Привязка формулы оплаты к преподавателю

**Дата**: 2025-11-16

### Что делает эта миграция?

Добавляет поле `formula_id` в таблицу `teachers`, чтобы формула оплаты была привязана к преподавателю, а не к каждому уроку отдельно. При выборе преподавателя формула будет подставляться автоматически.

---

## ⚡ БЫСТРЫЙ СПОСОБ (Рекомендуется)

### Через phpMyAdmin на хостинге Timeweb

**Шаг 1:** Зайдите в phpMyAdmin
- Откройте панель управления хостингом Timeweb
- Найдите раздел "Базы данных"
- Нажмите на кнопку "phpMyAdmin"

**Шаг 2:** Выберите базу данных
- В левом меню выберите базу `cw95865_admin`

**Шаг 3:** Выполните SQL команды
- Нажмите на вкладку "SQL" вверху
- Скопируйте и вставьте команды из файла `manual_migration.sql`
- **ВАЖНО:** Выполняйте команды **по одной**, последовательно:

```sql
-- Команда 1: Добавить столбец
ALTER TABLE `teachers`
ADD COLUMN `formula_id` INT NULL AFTER `email`;
```
👆 Нажмите "Вперёд", дождитесь успешного выполнения

```sql
-- Команда 2: Создать внешний ключ
ALTER TABLE `teachers`
ADD CONSTRAINT `fk_teachers_formula`
    FOREIGN KEY (`formula_id`)
    REFERENCES `payment_formulas`(`id`)
    ON DELETE SET NULL;
```
👆 Нажмите "Вперёд", дождитесь успешного выполнения

```sql
-- Команда 3: Создать индекс
ALTER TABLE `teachers`
ADD INDEX `idx_formula_id` (`formula_id`);
```
👆 Нажмите "Вперёд", дождитесь успешного выполнения

```sql
-- Команда 4: Мигрировать данные
UPDATE teachers t
LEFT JOIN (
    SELECT teacher_id, formula_id, COUNT(*) as cnt
    FROM lessons_template
    WHERE formula_id IS NOT NULL
    GROUP BY teacher_id, formula_id
    ORDER BY cnt DESC
) lt ON t.id = lt.teacher_id
SET t.formula_id = lt.formula_id
WHERE lt.formula_id IS NOT NULL;
```
👆 Нажмите "Вперёд" (эта команда перенесёт существующие формулы)

**Шаг 4:** Проверка результата
```sql
-- Проверочный запрос
SELECT t.id, t.name, t.formula_id, pf.name as formula_name
FROM teachers t
LEFT JOIN payment_formulas pf ON t.formula_id = pf.id
WHERE t.active = 1;
```
👆 Вы должны увидеть список преподавателей с их формулами

---

## 🔍 Проверка: Была ли миграция уже выполнена?

Если вы не уверены, выполните этот запрос в phpMyAdmin:

```sql
SHOW COLUMNS FROM teachers LIKE 'formula_id';
```

- **Если вернулся результат** → миграция уже выполнена, ничего делать не нужно
- **Если пусто** → выполните миграцию по инструкции выше

### Что изменится после миграции?

1. ✅ У каждого преподавателя будет своя формула оплаты
2. ✅ При создании урока формула будет подставляться автоматически из данных преподавателя
3. ✅ Существующие формулы из уроков будут автоматически присвоены преподавателям
4. ✅ Можно будет настраивать формулу для преподавателя на странице "Преподаватели"

### Проверка успешности миграции

После выполнения миграции выполните запрос:

```sql
SELECT t.name, pf.name as formula_name
FROM teachers t
LEFT JOIN payment_formulas pf ON t.formula_id = pf.id
WHERE t.active = 1;
```

Вы должны увидеть список преподавателей с их формулами оплаты.

---

## Миграция: Добавление номера кабинета к расписанию

**Дата**: 2025-11-17

### Что делает эта миграция?

Добавляет поле `room` в таблицу `lessons_template` для учёта номера кабинета, в котором проводится урок. Это позволяет отображать расписание в виде таблицы время × кабинет.

---

## ⚡ БЫСТРЫЙ СПОСОБ (Рекомендуется)

### Через phpMyAdmin на хостинге Timeweb

**Шаг 1:** Зайдите в phpMyAdmin
- Откройте панель управления хостингом Timeweb
- Найдите раздел "Базы данных"
- Нажмите на кнопку "phpMyAdmin"

**Шаг 2:** Выберите базу данных
- В левом меню выберите базу `cw95865_admin`

**Шаг 3:** Выполните SQL команды
- Нажмите на вкладку "SQL" вверху
- Скопируйте и вставьте команды из файла `add_room_to_lessons.sql`
- **ВАЖНО:** Выполняйте команды **по одной**, последовательно:

```sql
-- Команда 1: Добавить столбец room
ALTER TABLE `lessons_template`
ADD COLUMN `room` TINYINT(1) DEFAULT 1 AFTER `day_of_week`;
```
👆 Нажмите "Вперёд", дождитесь успешного выполнения

```sql
-- Команда 2: Обновить существующие записи
UPDATE `lessons_template`
SET `room` = 1
WHERE `room` IS NULL;
```
👆 Нажмите "Вперёд" (эта команда установит кабинет 1 для всех уроков)

**Шаг 4:** Проверка результата
```sql
-- Проверочный запрос
SELECT id, teacher_id, day_of_week, room, time_start, subject
FROM lessons_template
LIMIT 10;
```
👆 Вы должны увидеть список уроков с номерами кабинетов (1, 2 или 3)

---

## 🔍 Проверка: Была ли миграция уже выполнена?

Если вы не уверены, выполните этот запрос в phpMyAdmin:

```sql
SHOW COLUMNS FROM lessons_template LIKE 'room';
```

- **Если вернулся результат** → миграция уже выполнена, ничего делать не нужно
- **Если пусто** → выполните миграцию по инструкции выше

### Что изменится после миграции?

1. ✅ У каждого урока в расписании будет указан номер кабинета (1, 2 или 3)
2. ✅ Расписание будет отображаться в виде таблицы с временем и кабинетами
3. ✅ Можно будет фильтровать уроки по кабинетам
4. ✅ Существующие уроки автоматически получат кабинет №1

---

## Миграция: Добавление tier, grades, students к расписанию

**Дата**: 2025-11-17

### Что делает эта миграция?

Добавляет поля `tier` (уровень группы), `grades` (классы) и `students` (список учеников) в таблицу `lessons_template`.

---

## ⚡ БЫСТРЫЙ СПОСОБ (Рекомендуется)

### Через phpMyAdmin на хостинге Timeweb

**Шаг 1:** Зайдите в phpMyAdmin

**Шаг 2:** Выберите базу `cw95865_admin`

**Шаг 3:** Сначала проверьте, какие поля уже есть:

```sql
SHOW COLUMNS FROM lessons_template;
```

Посмотрите наличие полей: `tier`, `grades`, `students`. Если они уже есть - миграция не нужна.

**Шаг 4:** Если полей нет, выполните команды **по одной**:

```sql
-- Команда 1: Добавить поле tier
ALTER TABLE `lessons_template`
ADD COLUMN `tier` ENUM('S', 'A', 'B', 'C', 'D') DEFAULT 'C' AFTER `subject`;
```
👆 Нажмите "Вперёд", дождитесь успешного выполнения

```sql
-- Команда 2: Добавить поле grades
ALTER TABLE `lessons_template`
ADD COLUMN `grades` VARCHAR(50) NULL AFTER `tier`;
```
👆 Нажмите "Вперёд"

```sql
-- Команда 3: Добавить поле students
ALTER TABLE `lessons_template`
ADD COLUMN `students` TEXT NULL AFTER `grades`;
```
👆 Нажмите "Вперёд"

**Шаг 5:** Проверка результата

```sql
SELECT id, teacher_id, subject, tier, grades, students
FROM lessons_template
LIMIT 5;
```

### Что изменится после миграции?

1. ✅ Можно указывать уровень группы (тир S/A/B/C/D)
2. ✅ Можно указать классы (например "6, 7, 8")
3. ✅ Можно вести список учеников группы
4. ✅ Карточки уроков отображают всю информацию
5. ✅ Работает создание и редактирование уроков
