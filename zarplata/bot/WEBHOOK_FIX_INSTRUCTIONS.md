# Инструкции по завершению исправления Telegram Webhook

## Статус исправлений

✅ Все файлы исправлены и закоммичены:
- `webhook.php` - теперь возвращает HTTP 200 первым делом
- `AttendanceHandler.php` - добавлен fallback на formula_id
- `cleanup_updates.php` - скрипт для очистки старых записей
- Миграция `add_telegram_updates_table.sql` - создание таблицы

✅ Изменения запушены в ветку `claude/fix-telegram-webhook-01R6xwcUdtMMuojC1WGTBJ5F`

## Что нужно сделать вручную

### 1. Дождаться автоматического деплоя

GitHub Actions автоматически:
1. Смержит ветку `claude/fix-telegram-webhook-*` в `main`
2. Задеплоит изменения на Timeweb по FTP

**Проверить статус деплоя:**
https://github.com/Palomig/evrium/actions

### 2. Применить миграцию для создания таблицы telegram_updates

**Вариант А: Через phpMyAdmin**

1. Открыть phpMyAdmin на хостинге Timeweb
2. Выбрать базу данных `cw95865_admin`
3. Перейти на вкладку SQL
4. Скопировать и выполнить:

```sql
CREATE TABLE IF NOT EXISTS telegram_updates (
    id INT AUTO_INCREMENT PRIMARY KEY,
    update_id BIGINT NOT NULL COMMENT 'ID обновления от Telegram',
    created_at DATETIME NOT NULL COMMENT 'Дата и время получения обновления',
    UNIQUE KEY idx_update_id (update_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='История обработанных обновлений от Telegram бота';

CREATE INDEX idx_created_at ON telegram_updates(created_at);
```

**Вариант Б: Через SSH (если есть доступ)**

```bash
mysql -u cw95865_admin -p123456789 cw95865_admin < /путь/к/zarplata/migrations/add_telegram_updates_table.sql
```

### 3. Сбросить webhook Telegram и очистить очередь

**ПОСЛЕ того как деплой завершился и миграция применена:**

1. Открыть в браузере:
   ```
   https://эвриум.рф/zarplata/bot/reset_webhook.php
   ```

2. Скрипт автоматически:
   - Удалит старый webhook
   - Очистит очередь pending updates
   - Установит webhook заново
   - Покажет финальный статус

3. Вы увидите сообщение `✅ Reset completed`

### 4. Проверить работу бота

1. Открыть Telegram
2. Отправить боту команду `/start`
3. Бот должен ответить с кнопками меню
4. Нажать "📅 Сегодня" - должен показать список уроков
5. Нажать кнопку "Все пришли" - должен начислить зарплату

### 5. Настроить cron для очистки старых записей (опционально)

**Через cPanel на Timeweb:**

1. Раздел "Cron Jobs" (Задания Cron)
2. Добавить новое задание:
   - **Периодичность**: Ежедневно в 3:00 ночи
   - **Команда**:
     ```
     /usr/bin/php /путь/к/zarplata/bot/cleanup_updates.php
     ```

**Или вручную добавить в crontab:**
```
0 3 * * * /usr/bin/php /home/cw95865/PALOMATIKA/public_html/zarplata/bot/cleanup_updates.php
```

## Проверка что проблема решена

После всех шагов:

1. ✅ Webhook всегда возвращает HTTP 200 (даже при ошибках)
2. ✅ Telegram НЕ повторяет запросы
3. ✅ Дубли update_id отфильтровываются
4. ✅ Ошибки в AttendanceHandler не падают с 500
5. ✅ Старые записи telegram_updates автоматически удаляются

**Проверить логи ошибок:**
- `/var/log/php_errors.log` - должны исчезнуть ошибки 500
- Telegram webhook info - pending_update_count должен быть 0

## В случае проблем

Если бот всё ещё не работает:

1. Проверить логи: `tail -f /var/log/php_errors.log`
2. Проверить webhook info: https://api.telegram.org/bot{TOKEN}/getWebhookInfo
3. Открыть тестовый файл: https://эвриум.рф/zarplata/bot/test_webhook.php

## Удаление служебных файлов (после проверки)

После успешного тестирования можно удалить:
- `zarplata/bot/reset_webhook.php` (для безопасности)
- `zarplata/bot/test_webhook.php` (для безопасности)
- `zarplata/bot/WEBHOOK_FIX_INSTRUCTIONS.md` (эту инструкцию)

---

**Дата создания**: 2025-11-27
**Автор**: Claude AI
**Коммит**: 54d6bbf
