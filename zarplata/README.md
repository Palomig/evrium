# Система учёта зарплаты преподавателей

**Версия:** 1.0
**Дата:** 2025-11-15
**Автор:** Evrium Development Team
**Домен:** https://эвриум.рф/zarplata

---

## 📋 Описание проекта

Полнофункциональная система автоматизации учёта посещаемости и начисления зарплаты преподавателям с интеграцией Telegram-бота. Система предоставляет удобный веб-интерфейс для владельца и автоматизированное управление через Telegram для преподавателей.

### Основные возможности

- ✅ **Автоматический учёт посещаемости** через Telegram-бот
- ✅ **Гибкие формулы расчёта зарплаты** (min_plus_per, fixed, expression)
- ✅ **Управление расписанием** (шаблоны + инстансы)
- ✅ **Временные переносы уроков** без изменения основного расписания
- ✅ **Система замещений** преподавателей
- ✅ **Циклы выплат** (раз в месяц, два раза в месяц, разовые)
- ✅ **Подробные отчёты** для владельца и преподавателей
- ✅ **Полный audit log** всех операций
- ✅ **Material Design** тёмная тема UI/UX

---

## 🛠️ Технологический стек

### Backend
- **PHP 8.x** - серверная логика
- **MySQL 5.7+** - база данных
- **PDO** - работа с БД (подготовленные запросы)
- **Telegram Bot API** - интеграция с Telegram

### Frontend
- **Material Design** - дизайн-система от Google
- **Vanilla JavaScript** - интерактивность
- **Google Fonts** (Roboto) - типографика
- **Material Icons** - иконки

### DevOps
- **Apache** - веб-сервер
- **Shared Hosting** - Timeweb
- **GitHub Actions** - CI/CD (авто-деплой)

---

## 📂 Структура проекта

```
zarplata/
├── 📁 config/                  # Конфигурационные файлы
│   ├── db.php                  # Работа с базой данных
│   ├── auth.php                # Аутентификация и авторизация
│   └── helpers.php             # Вспомогательные функции
│
├── 📁 api/                     # REST API эндпоинты (будущее)
│   ├── teachers.php            # API для преподавателей
│   ├── lessons.php             # API для уроков
│   ├── payments.php            # API для выплат
│   └── reports.php             # API для отчётов
│
├── 📁 bot/                     # Telegram Bot
│   ├── webhook.php             # Webhook обработчик
│   ├── cron.php                # Cron задачи (опрос посещаемости)
│   └── handlers/               # Обработчики команд бота
│
├── 📁 assets/                  # Статические ресурсы
│   ├── css/
│   │   └── material-dark.css   # Material Design тёмная тема
│   ├── js/
│   └── images/
│
├── 📁 templates/               # Шаблоны (опционально)
│
├── index.php                   # Главная страница (Dashboard)
├── login.php                   # Страница входа
├── logout.php                  # Выход из системы
├── teachers.php                # Управление преподавателями
├── schedule.php                # Управление расписанием
├── lessons.php                 # Список уроков
├── payments.php                # Управление выплатами
├── reports.php                 # Отчёты
├── formulas.php                # Формулы оплаты
├── settings.php                # Настройки системы
│
├── database.sql                # Схема базы данных
├── .htaccess                   # Конфигурация Apache
└── README.md                   # Документация (этот файл)
```

---

## 🚀 Установка и настройка

### Шаг 1: Требования

- **PHP** >= 8.0
- **MySQL** >= 5.7 или MariaDB >= 10.3
- **Apache** с mod_rewrite
- **Composer** (опционально, для зависимостей)
- **Telegram Bot Token** (для интеграции)

### Шаг 2: Клонирование и размещение

1. Разместить папку `zarplata/` в корне сайта:
   ```
   /home/user/public_html/zarplata/
   ```

2. Убедиться, что сайт доступен по адресу:
   ```
   https://эвриум.рф/zarplata
   ```

### Шаг 3: Создание базы данных

1. Создать базу данных:
   ```sql
   CREATE DATABASE evrium_zarplata CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

2. Импортировать схему:
   ```bash
   mysql -u root -p evrium_zarplata < database.sql
   ```

3. Проверить создание таблиц:
   ```sql
   USE evrium_zarplata;
   SHOW TABLES;
   ```

### Шаг 4: Настройка конфигурации

Отредактировать файл `config/db.php`:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'evrium_zarplata');
define('DB_USER', 'your_db_user');
define('DB_PASS', 'your_db_password');
```

### Шаг 5: Настройка Apache

Убедиться, что mod_rewrite включён:

```bash
a2enmod rewrite
systemctl restart apache2
```

Проверить, что `.htaccess` работает (должны быть чистые URL без .php).

### Шаг 6: Установка прав доступа

```bash
chmod 755 zarplata/
chmod 644 zarplata/*.php
chmod 644 zarplata/config/*.php
chmod 600 zarplata/.htaccess
```

### Шаг 7: Первый вход

1. Открыть в браузере:
   ```
   https://эвриум.рф/zarplata/login.php
   ```

2. Войти с учётными данными по умолчанию:
   - **Логин:** `admin`
   - **Пароль:** `admin123`

3. **ВАЖНО:** Сразу после входа изменить пароль!

---

## 🗄️ База данных

### Основные таблицы

#### `users` - Администраторы
Владельцы и администраторы системы.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INT | Первичный ключ |
| username | VARCHAR(50) | Логин (уникальный) |
| password_hash | VARCHAR(255) | Хеш пароля (bcrypt) |
| name | VARCHAR(100) | Полное имя |
| role | ENUM | Роль: admin, owner |
| active | BOOLEAN | Активность аккаунта |

#### `teachers` - Преподаватели
Все преподаватели в системе.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INT | Первичный ключ |
| name | VARCHAR(100) | ФИО преподавателя |
| telegram_id | BIGINT | ID в Telegram (уникальный) |
| telegram_username | VARCHAR(50) | Username в Telegram |
| phone | VARCHAR(20) | Телефон |
| active | BOOLEAN | Активность |

#### `students` - Ученики
Список всех учеников.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INT | Первичный ключ |
| name | VARCHAR(100) | ФИО ученика |
| class | INT | Класс (7-11) |
| phone | VARCHAR(20) | Телефон |
| parent_phone | VARCHAR(20) | Телефон родителя |

#### `payment_formulas` - Формулы оплаты
Конфигурируемые формулы расчёта зарплаты.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INT | Первичный ключ |
| name | VARCHAR(100) | Название формулы |
| type | ENUM | Тип: min_plus_per, fixed, expression |
| min_payment | INT | Минимальная оплата (₽) |
| per_student | INT | Доплата за ученика (₽) |
| threshold | INT | С какого ученика доплата |
| fixed_amount | INT | Фиксированная сумма (₽) |
| expression | TEXT | Пользовательское выражение |

#### `lessons_template` - Шаблоны расписания
Еженедельно повторяющееся расписание.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INT | Первичный ключ |
| teacher_id | INT | ID преподавателя (FK) |
| day_of_week | TINYINT | День недели (1=Пн, 7=Вс) |
| time_start | TIME | Время начала |
| time_end | TIME | Время окончания |
| lesson_type | ENUM | Тип: group, individual |
| expected_students | INT | Ожидаемое кол-во учеников |
| formula_id | INT | ID формулы оплаты (FK) |

#### `lessons_instance` - Инстансы уроков
Конкретные занятия (из шаблона или разовые).

| Поле | Тип | Описание |
|------|-----|----------|
| id | INT | Первичный ключ |
| template_id | INT | ID шаблона (FK, NULL для разовых) |
| teacher_id | INT | ID преподавателя (FK) |
| substitute_teacher_id | INT | ID замещающего (FK, NULL) |
| lesson_date | DATE | Дата урока |
| time_start | TIME | Время начала |
| time_end | TIME | Время окончания |
| expected_students | INT | Ожидаемое кол-во |
| actual_students | INT | Фактическое кол-во |
| status | ENUM | Статус: scheduled, completed, cancelled, rescheduled |

#### `attendance_log` - Журнал посещаемости
Кто пришёл на какой урок.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INT | Первичный ключ |
| lesson_instance_id | INT | ID урока (FK) |
| student_id | INT | ID ученика (FK) |
| attended | BOOLEAN | Присутствовал ли |
| marked_at | DATETIME | Время отметки |
| marked_by | VARCHAR(50) | Кто отметил (telegram_bot / admin) |

#### `payments` - Начисления зарплаты
Все начисления преподавателям.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INT | Первичный ключ |
| teacher_id | INT | ID преподавателя (FK) |
| lesson_instance_id | INT | ID урока (FK, NULL для разовых) |
| amount | INT | Сумма в рублях |
| payment_type | ENUM | Тип: lesson, bonus, penalty, adjustment |
| status | ENUM | Статус: pending, approved, paid, cancelled |
| paid_at | DATETIME | Дата выплаты |

#### `audit_log` - Журнал аудита
История всех действий в системе.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INT | Первичный ключ |
| action_type | VARCHAR(50) | Тип действия |
| entity_type | VARCHAR(50) | Тип сущности |
| entity_id | INT | ID сущности |
| user_id | INT | ID пользователя (FK) |
| old_value | TEXT | Старое значение (JSON) |
| new_value | TEXT | Новое значение (JSON) |
| notes | TEXT | Примечания |
| created_at | DATETIME | Время события |

### Представления (Views)

#### `teacher_stats` - Статистика по преподавателям
Агрегированная статистика: уроки, заработок, выплаты.

#### `lessons_stats` - Статистика по урокам
Детальная статистика по всем урокам с выплатами.

### Триггеры

#### `calculate_payment_after_lesson_complete`
Автоматически рассчитывает зарплату при завершении урока.

#### `audit_attendance_log`
Автоматически логирует отметки посещаемости в audit_log.

---

## 🔐 Безопасность

### Аутентификация

- **Хеширование паролей:** `password_hash()` с bcrypt
- **Проверка паролей:** `password_verify()`
- **Сессии:** HTTPOnly, Secure, SameSite=Strict
- **CSRF защита:** Токены в формах

### SQL Injection

- **100% Prepared Statements** - все запросы через PDO
- **Параметризованные запросы** - никогда не конкатенируем SQL
- **Валидация входных данных** - проверка типов и форматов

### XSS Protection

- **Экранирование вывода:** `htmlspecialchars()` везде
- **Content Security Policy** в `.htaccess`
- **X-XSS-Protection** заголовок

### Прочее

- **X-Frame-Options:** SAMEORIGIN - защита от clickjacking
- **X-Content-Type-Options:** nosniff
- **Скрытие версии PHP** - `expose_php = Off`
- **Логирование ошибок** - без вывода пользователю

---

## 📱 Telegram Bot

### Настройка

1. Создать бота через [@BotFather](https://t.me/BotFather)
2. Получить токен
3. Добавить токен в настройки системы (`settings` таблица)
4. Настроить webhook:
   ```bash
   curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook" \
     -d "url=https://эвриум.рф/zarplata/bot/webhook.php"
   ```

### Функционал бота

#### Для преподавателей:

**Опрос посещаемости:**
- Бот спрашивает через 15 минут после начала урока
- "Все ли пришли?"
- Если "Нет" → показывает список учеников
- Преподаватель отмечает отсутствующих
- Система автоматически рассчитывает оплату

**Команды:**
- `/start` - Приветствие и инструкции
- `/today` - Сколько заработано сегодня
- `/week` - Сколько заработано за неделю
- `/schedule` - Расписание на сегодня/неделю
- `/help` - Помощь

#### Для владельца:

- Получение отчётов в Telegram
- Уведомления о важных событиях
- Быстрая статистика

### Cron задача

Добавить в crontab для проверки уроков каждые 5 минут:

```cron
*/5 * * * * php /path/to/zarplata/bot/cron.php >> /var/log/zarplata_cron.log 2>&1
```

---

## 💰 Формулы оплаты

### Типы формул

#### 1. min_plus_per (Минимум + Доплата)

**Описание:** Базовая сумма + доплата за каждого ученика начиная с N-го.

**Параметры:**
- `min_payment` - Минимальная оплата (₽)
- `per_student` - Доплата за ученика (₽)
- `threshold` - С какого ученика доплата (по умолчанию 2)

**Пример:**
```
min_payment = 500₽
per_student = 150₽
threshold = 2

1 ученик: 500₽
2 ученика: 500 + 150 = 650₽
3 ученика: 500 + 150*2 = 800₽
4 ученика: 500 + 150*3 = 950₽
```

#### 2. fixed (Фиксированная ставка)

**Описание:** Одна и та же сумма независимо от количества учеников.

**Параметры:**
- `fixed_amount` - Фиксированная сумма (₽)

**Пример:**
```
fixed_amount = 800₽

Любое кол-во учеников: 800₽
```

#### 3. expression (Пользовательская формула)

**Описание:** Произвольное математическое выражение.

**Переменные:**
- `N` - Количество учеников
- `min` - Минимальная оплата
- `base` - Базовая ставка

**Функции:**
- `max(a, b)` - Максимум из двух значений
- `min(a, b)` - Минимум из двух значений

**Примеры:**
```
"max(500, N * 150)" → не меньше 500₽, иначе 150₽ за ученика
"N * 200 + 300" → 200₽ за ученика + 300₽ базовая
"min + (N - 1) * 100" → используя переменную min
```

---

## 📊 Отчёты

### Для владельца

**В веб-панели:**
- Dashboard с общей статистикой
- Заработок преподавателей (текущий период)
- Ваша доля от выручки
- График динамики по дням
- Фильтры по преподавателю и периоду

**В Telegram:**
- Текстовые отчёты по запросу
- Ежедневная сводка
- Уведомления о важных событиях

### Для преподавателя

**В Telegram:**
- Сколько заработано сегодня
- Сколько заработано за неделю
- Детализация по урокам
- История выплат

---

## 🔧 API (Планируется)

### Аутентификация

**Метод:** Bearer Token в заголовке `Authorization`

```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  https://эвриум.рф/zarplata/api/teachers.php?action=list
```

### Эндпоинты

#### Teachers API

- `GET /api/teachers.php?action=list` - Список преподавателей
- `GET /api/teachers.php?action=get&id=5` - Один преподаватель
- `POST /api/teachers.php?action=add` - Добавить преподавателя
- `POST /api/teachers.php?action=update` - Обновить преподавателя
- `POST /api/teachers.php?action=delete` - Удалить преподавателя

#### Lessons API

- `GET /api/lessons.php?action=list` - Список уроков
- `GET /api/lessons.php?action=get&id=10` - Один урок
- `POST /api/lessons.php?action=add` - Добавить урок
- `POST /api/lessons.php?action=complete` - Завершить урок
- `POST /api/lessons.php?action=cancel` - Отменить урок
- `POST /api/lessons.php?action=reschedule` - Перенести урок

#### Payments API

- `GET /api/payments.php?action=list` - Список выплат
- `GET /api/payments.php?action=stats` - Статистика
- `POST /api/payments.php?action=approve` - Одобрить выплату
- `POST /api/payments.php?action=mark_paid` - Отметить как выплачено

---

## 🎨 UI/UX

### Material Design Dark Theme

**Цветовая палитра:**
- **Primary:** #BB86FC (Фиолетовый)
- **Secondary:** #03DAC6 (Бирюзовый)
- **Background:** #121212 (Чёрный)
- **Surface:** #1E1E1E (Тёмно-серый)
- **Error:** #CF6679 (Красный)
- **Success:** #4CAF50 (Зелёный)
- **Warning:** #FF9800 (Оранжевый)
- **Info:** #2196F3 (Синий)

**Типографика:**
- **Font:** Roboto
- **Sizes:** H1 (2.5rem), H2 (2rem), H3 (1.75rem), Body (1rem)
- **Weights:** 300 (Light), 400 (Regular), 500 (Medium), 700 (Bold)

**Компоненты:**
- Cards с elevation shadows
- Material Icons
- Floating labels на input
- Ripple эффект на кнопках
- Smooth transitions

---

## 🚢 Деплой

### Через GitHub Actions (Автоматический)

1. Коммит в ветку `claude/*`
2. Авто-мердж в `main`
3. FTP деплой на Timeweb

### Ручной деплой

1. Скопировать файлы на сервер через FTP
2. Импортировать БД
3. Настроить `config/db.php`
4. Проверить права доступа
5. Тестировать

---

## 📞 Поддержка

**Документация:** Этот файл
**Техническое задание:** `/docs/zarplata.md`
**Email:** support@evrium.ru
**GitHub Issues:** [Создать issue](https://github.com/Palomig/evrium/issues)

---

## 📄 Лицензия

© 2025 Evrium Development Team. Все права защищены.

---

## ✅ TODO (Будущие улучшения)

- [ ] Реализовать REST API
- [ ] Создать Telegram Bot (webhook + cron)
- [ ] Добавить страницы: teachers.php, schedule.php, lessons.php, payments.php, reports.php, formulas.php, settings.php
- [ ] Реализовать импорт расписания из Google Sheets
- [ ] Добавить генерацию PDF отчётов
- [ ] Создать дашборд с графиками (Chart.js)
- [ ] Реализовать drag-and-drop для переноса уроков
- [ ] Добавить мобильную версию (responsive)
- [ ] Создать PWA (Progressive Web App)
- [ ] Добавить экспорт в Excel
- [ ] Интеграция с платёжными системами (опционально)
- [ ] Multi-tenancy поддержка (несколько школ)

---

**Последнее обновление:** 2025-11-15
**Версия документации:** 1.0
