# Zarplata - Оставшиеся задачи для завершения проекта

**Последнее обновление**: 2025-11-17
**Статус проекта**: ~75% готовности
**Основные компоненты**: ✅ Schedule, ✅ Teachers, ✅ Formulas, ⚠️ Lessons, ⚠️ Payments, ❌ Reports, ❌ Telegram Bot

---

## 📊 Общий прогресс

```
ГОТОВО:      ████████████░░░░░░░░ 60%
В РАЗРАБОТКЕ: ████░░░░░░░░░░░░░░░░ 20%
НЕ НАЧАТО:    ████░░░░░░░░░░░░░░░░ 20%
```

---

## ✅ Что УЖЕ реализовано

### Backend (PHP)
- ✅ Система аутентификации (login/logout)
- ✅ Управление пользователями (users table)
- ✅ CRUD преподавателей (teachers)
- ✅ CRUD формул оплаты (payment_formulas)
- ✅ **Система расписания** (lessons_template)
  - ✅ Шаблоны уроков с кабинетами (1-3)
  - ✅ Tier-система (S/A/B/C/D)
  - ✅ Поддержка классов (grades)
  - ✅ JSON списки учеников
  - ✅ API для CRUD операций
- ✅ Базовая структура инстансов уроков (lessons_instance)
- ✅ Audit log система
- ✅ Settings управление
- ✅ Database helpers (dbQuery, dbExecute, dbQueryOne)
- ✅ Helper functions (jsonSuccess, jsonError, logAudit)

### Frontend (Material Design)
- ✅ Тёмная тема Material Design
- ✅ Фиксированный sidebar (280px)
- ✅ **Страница расписания** (schedule.php)
  - ✅ Табличный layout (Время × Кабинеты)
  - ✅ Карточки уроков с тирами
  - ✅ Модальное окно добавления/редактирования
  - ✅ Удаление шаблонов
  - ✅ Фильтры (дни, кабинеты, время)
  - ✅ Spoiler для списка учеников
  - ✅ Отображение "Нет занятий" для пустых дней
- ✅ Страница преподавателей (teachers.php)
- ✅ Страница формул (formulas.php)
- ✅ Базовая структура страницы уроков (lessons.php)
- ✅ Базовая структура страницы выплат (payments.php)

### Database
- ✅ Все 10 таблиц созданы
- ✅ Views (teacher_stats, lessons_stats)
- ✅ Triggers (calculate_payment, audit_attendance)
- ✅ Миграции для room, tier, grades, students полей
- ✅ Backwards compatibility в API

---

## 🚧 В РАЗРАБОТКЕ (нужно доделать)

### 1. Lessons Instance Management (lessons.php) - 40% готово

**Что есть**:
- ✅ Базовая страница с таблицей
- ✅ Получение данных из БД
- ✅ Отображение уроков

**Что нужно**:
- ❌ Модальное окно для редактирования урока
- ❌ Изменение статуса урока (scheduled → completed/cancelled)
- ❌ Поле для ввода фактического количества учеников
- ❌ Поддержка замещающего преподавателя (substitute_teacher_id)
- ❌ Фильтры (по дате, преподавателю, статусу)
- ❌ Пагинация для больших списков
- ❌ Связь с attendance_log (посещаемость)

**API эндпоинты** (api/lessons.php):
- ⚠️ `list` - Частично реализован
- ❌ `get` - Не реализован
- ❌ `update` - Не реализован
- ❌ `complete` - Отметить как проведённый
- ❌ `cancel` - Отменить урок
- ❌ `reschedule` - Перенести урок

**Приоритет**: 🔥 ВЫСОКИЙ

---

### 2. Payments System (payments.php) - 30% готово

**Что есть**:
- ✅ Базовая страница
- ✅ Таблица payments в БД
- ✅ Trigger для автоматического расчёта (не полностью реализован)

**Что нужно**:
- ❌ Полная реализация триггера `calculate_payment_after_lesson_complete`
  - Получить formula_id (из урока или преподавателя)
  - Рассчитать сумму на основе типа формулы:
    - `min_plus_per`: базовая + доплата * (students - threshold)
    - `fixed`: фиксированная сумма
    - `expression`: eval математического выражения с N=actual_students
  - Создать запись в payments
- ❌ Страница истории выплат
- ❌ Фильтры (по преподавателю, периоду, статусу)
- ❌ Кнопки "Approve" / "Mark as Paid"
- ❌ Ручное добавление платежа (bonus/penalty/adjustment)
- ❌ Экспорт в Excel/PDF

**API эндпоинты** (api/payments.php):
- ⚠️ `list` - Частично реализован
- ❌ `approve` - Одобрить выплату
- ❌ `mark_paid` - Отметить как выплачено
- ❌ `add_manual` - Ручное добавление
- ❌ `stats` - Статистика по периоду

**Приоритет**: 🔥 ВЫСОКИЙ

---

### 3. Reports Generation (reports.php) - 10% готово

**Что есть**:
- ✅ Пустая страница

**Что нужно**:
- ❌ **Отчёт по преподавателю**:
  - Количество уроков (всего / проведено / отменено)
  - Сумма к выплате (всего / выплачено / в ожидании)
  - Средняя посещаемость
  - График по дням/неделям
- ❌ **Отчёт по периоду**:
  - Общая статистика
  - Топ преподавателей
  - Финансовая сводка
- ❌ **Отчёт по ученику** (если нужно):
  - Посещаемость
  - Уроки
- ❌ Фильтры (период, преподаватель)
- ❌ Экспорт в PDF
- ❌ Экспорт в Excel (CSV)

**API эндпоинты** (api/reports.php):
- ❌ `teacher_report` - Отчёт преподавателя
- ❌ `period_report` - Отчёт за период
- ❌ `export_pdf` - PDF экспорт
- ❌ `export_csv` - CSV экспорт

**Библиотеки для PDF**:
- TCPDF или FPDF (PHP)
- Или использовать HTML → PDF конвертер

**Приоритет**: 🔶 СРЕДНИЙ

---

## ❌ НЕ НАЧАТО (будущие фичи)

### 4. Telegram Bot Integration - 0% готово

**Папка**: `/zarplata/bot/`

**Что нужно создать**:

#### 4.1 Webhook Handler (`bot/webhook.php`)
```php
<?php
// Получение обновлений от Telegram
// Роутинг команд к обработчикам
```

**Команды для преподавателей**:
- `/start` - Привязка telegram_id к аккаунту преподавателя
- `/today` - Сколько заработано сегодня
- `/week` - Сколько заработано за неделю
- `/schedule` - Расписание на сегодня/неделю
- `/help` - Помощь

**Опрос посещаемости**:
- Через 15 минут после начала урока бот спрашивает:
  - "Все пришли на урок?"
  - Если "Нет" → показывает список учеников
  - Преподаватель отмечает отсутствующих
  - Система сохраняет в attendance_log
  - Триггер рассчитывает зарплату

#### 4.2 Cron Task (`bot/cron.php`)
```php
<?php
// Запускается каждые 5 минут
// Проверяет уроки, которые начались 15 минут назад
// Отправляет опрос преподавателю в Telegram
```

**Crontab**:
```cron
*/5 * * * * php /path/to/zarplata/bot/cron.php >> /var/log/zarplata_cron.log 2>&1
```

#### 4.3 Command Handlers (`bot/handlers/`)
```
bot/handlers/
├── StartCommand.php      # /start - регистрация
├── TodayCommand.php      # /today - заработок сегодня
├── WeekCommand.php       # /week - заработок за неделю
├── ScheduleCommand.php   # /schedule - расписание
└── HelpCommand.php       # /help - помощь
```

**Settings**:
- `telegram_bot_token` - токен бота (settings table)
- `telegram_webhook_url` - URL webhook

**Приоритет**: 🔶 СРЕДНИЙ (можно сделать после Reports)

---

### 5. Attendance System - 20% готово

**Что есть**:
- ✅ Таблица attendance_log
- ✅ Trigger для audit

**Что нужно**:
- ❌ Страница для ручного ввода посещаемости (если Telegram bot не используется)
- ❌ API для отметки посещаемости
  - `POST /api/attendance.php?action=mark`
  - Параметры: lesson_instance_id, student_id, attended (true/false)
- ❌ Интеграция с Telegram bot
- ❌ Автоматический расчёт actual_students на основе attendance_log

**Приоритет**: 🔶 СРЕДНИЙ (зависит от Telegram bot)

---

### 6. Dashboard Improvements (index.php) - 50% готово

**Что есть**:
- ✅ Базовая статистика
- ✅ Подсчёт уроков
- ✅ Pending payments

**Что нужно**:
- ❌ **Виджеты**:
  - Уроки сегодня (список с преподавателями)
  - Зарплата к выплате (по преподавателям)
  - График динамики уроков (Chart.js)
  - График финансов (Chart.js)
- ❌ **Quick actions**:
  - "Сгенерировать уроки на неделю"
  - "Посмотреть сегодняшние уроки"
- ❌ Календарь с уроками (опционально)

**Приоритет**: 🔵 НИЗКИЙ (косметика)

---

### 7. Students Management - 0% готово

**Проблема**: Сейчас студенты хранятся как JSON массив в TEXT поле `lessons_template.students`

**Варианты решения**:

#### Вариант A: Оставить как есть (простой)
- ✅ Просто и быстро
- ✅ Подходит для небольших списков
- ❌ Нельзя делать JOIN запросы
- ❌ Нет полноценной работы с учениками

#### Вариант B: Создать полноценную таблицу students (сложный)
- ✅ Полноценная CRUD система
- ✅ JOIN запросы и аналитика
- ✅ Связь students ↔ lessons через lesson_students таблицу
- ❌ Больше кода и сложности

**Рекомендация**: Вариант A для MVP, Вариант B для полноценной системы

**Если выбрать Вариант B**:
- ❌ Страница students.php (CRUD)
- ❌ API api/students.php
- ❌ Таблица lesson_students (many-to-many)
- ❌ Миграция JSON → relational data

**Приоритет**: 🔵 НИЗКИЙ (или оставить как есть)

---

### 8. Settings Page Improvements - 60% готово

**Что есть**:
- ✅ Базовая страница settings.php
- ✅ Таблица settings в БД
- ✅ API для чтения/записи

**Что нужно**:
- ❌ UI для изменения настроек:
  - Telegram bot token
  - Webhook URL
  - Email для уведомлений
  - Timezone
  - Валюта (₽, $, €)
- ❌ Группировка настроек по категориям
- ❌ Validation для каждого типа настройки

**Приоритет**: 🔵 НИЗКИЙ

---

### 9. Security & Validation - 70% готово

**Что есть**:
- ✅ Prepared statements (PDO)
- ✅ htmlspecialchars для вывода
- ✅ filter_var для валидации
- ✅ Session-based auth

**Что нужно улучшить**:
- ⚠️ CSRF токены для всех форм
- ⚠️ Rate limiting для API
- ⚠️ Input validation на фронтенде (JavaScript)
- ⚠️ Strong password policy (опционально)
- ⚠️ 2FA для владельца (опционально)
- ⚠️ Логирование попыток входа (audit_log)

**Приоритет**: 🔶 СРЕДНИЙ

---

### 10. UI/UX Improvements

**Что можно улучшить**:
- ❌ Responsive design (мобильная версия)
- ❌ Loading spinners при API запросах
- ❌ Toast notifications вместо alert()
- ❌ Drag & drop для переноса уроков в расписании
- ❌ Keyboard shortcuts
- ❌ Dark/Light theme toggle (сейчас только тёмная)
- ❌ Анимации и transitions

**Приоритет**: 🔵 НИЗКИЙ (косметика)

---

## 🎯 Рекомендуемый порядок реализации

### Фаза 1: Завершение базового функционала (2-3 дня)
1. ✅ **Lessons Instance Management** - КРИТИЧНО
   - Редактирование уроков
   - Смена статуса (completed/cancelled)
   - Фактическое количество учеников
2. ✅ **Payments Calculation Trigger** - КРИТИЧНО
   - Полная реализация триггера
   - Расчёт по всем типам формул
3. ✅ **Payments Page** - КРИТИЧНО
   - История выплат
   - Approve/Mark as Paid
   - Фильтры

### Фаза 2: Отчёты и аналитика (1-2 дня)
4. ✅ **Reports Generation**
   - Отчёт по преподавателю
   - Отчёт по периоду
   - PDF/CSV экспорт

### Фаза 3: Автоматизация (3-5 дней)
5. ✅ **Telegram Bot** (опционально, но очень полезно)
   - Webhook handler
   - Cron для опроса
   - Команды /today, /week, /schedule
   - Опрос посещаемости

### Фаза 4: Улучшения (1-2 дня)
6. ✅ **Dashboard виджеты**
7. ✅ **Settings UI**
8. ✅ **Security improvements**

### Фаза 5: Полировка (опционально)
9. ✅ Students Management (если нужно)
10. ✅ UI/UX improvements

---

## 📋 Детальный TODO по файлам

### `/zarplata/lessons.php` (страница)
```
[ ] Добавить модальное окно редактирования урока
[ ] Кнопка "Отметить как проведённый"
[ ] Кнопка "Отменить урок"
[ ] Поле для ввода фактического количества учеников
[ ] Селект для замещающего преподавателя
[ ] Фильтры (дата, преподаватель, статус)
[ ] Пагинация (показывать по 50 уроков)
```

### `/zarplata/api/lessons.php` (API)
```
[ ] action=list (с фильтрами)
[ ] action=get (по ID)
[ ] action=update (изменить данные урока)
[ ] action=complete (отметить как проведённый + указать actual_students)
[ ] action=cancel (отменить урок)
[ ] action=reschedule (перенести на другую дату/время)
```

### `/zarplata/payments.php` (страница)
```
[ ] Таблица истории выплат
[ ] Кнопка "Approve" для pending платежей
[ ] Кнопка "Mark as Paid"
[ ] Модальное окно ручного добавления платежа
[ ] Фильтры (преподаватель, период, статус)
[ ] Сортировка по дате/сумме
```

### `/zarplata/api/payments.php` (API)
```
[ ] action=list (с фильтрами)
[ ] action=approve (изменить status на 'approved')
[ ] action=mark_paid (изменить status на 'paid', установить paid_at)
[ ] action=add_manual (добавить bonus/penalty/adjustment)
[ ] action=stats (статистика: всего/выплачено/в ожидании)
```

### Триггер `calculate_payment_after_lesson_complete`
```sql
CREATE TRIGGER calculate_payment_after_lesson_complete
AFTER UPDATE ON lessons_instance
FOR EACH ROW
BEGIN
    IF NEW.status = 'completed' AND OLD.status != 'completed' THEN
        -- Получить formula_id (приоритет: урок → преподаватель)
        SET @formula_id = COALESCE(
            NEW.formula_id,
            (SELECT formula_id FROM teachers WHERE id = NEW.teacher_id)
        );

        -- Если есть формула
        IF @formula_id IS NOT NULL THEN
            -- Получить данные формулы
            SELECT type, min_payment, per_student, threshold, fixed_amount, expression
            INTO @type, @min, @per, @thresh, @fixed, @expr
            FROM payment_formulas
            WHERE id = @formula_id;

            -- Рассчитать сумму
            SET @amount = CASE @type
                WHEN 'min_plus_per' THEN
                    @min + GREATEST(0, NEW.actual_students - @thresh) * @per
                WHEN 'fixed' THEN
                    @fixed
                WHEN 'expression' THEN
                    -- TODO: Eval expression (сложно в MySQL, проще в PHP)
                    @fixed
                ELSE 0
            END;

            -- Создать запись о выплате
            INSERT INTO payments (teacher_id, lesson_instance_id, amount, payment_type, status)
            VALUES (NEW.teacher_id, NEW.id, @amount, 'lesson', 'pending');
        END IF;
    END IF;
END;
```

### `/zarplata/reports.php` (страница)
```
[ ] Форма выбора периода и преподавателя
[ ] Виджет "Статистика преподавателя"
[ ] Виджет "Финансовая сводка"
[ ] График уроков (Chart.js)
[ ] Кнопка "Экспорт в PDF"
[ ] Кнопка "Экспорт в CSV"
```

### `/zarplata/api/reports.php` (API)
```
[ ] action=teacher_report (отчёт преподавателя)
[ ] action=period_report (отчёт за период)
[ ] action=export_pdf (генерация PDF)
[ ] action=export_csv (генерация CSV)
```

### `/zarplata/bot/webhook.php` (Telegram Bot)
```
[ ] Получение JSON от Telegram
[ ] Роутинг команд
[ ] Обработка /start
[ ] Обработка /today
[ ] Обработка /week
[ ] Обработка /schedule
[ ] Обработка /help
[ ] Обработка callback buttons (для опроса)
```

### `/zarplata/bot/cron.php` (Cron задача)
```
[ ] Получить уроки, которые начались 15 минут назад
[ ] Для каждого урока:
    [ ] Проверить, есть ли telegram_id у преподавателя
    [ ] Отправить сообщение с вопросом о посещаемости
    [ ] Если есть список учеников → показать inline keyboard
[ ] Сохранить отметку, что опрос отправлен
```

---

## 🧪 Тестирование

**Перед деплоем проверить**:
- [ ] Создание/редактирование/удаление шаблонов расписания
- [ ] Генерация недели из шаблонов
- [ ] Создание/редактирование преподавателей
- [ ] Создание/редактирование формул
- [ ] Завершение урока → автоматическое создание payment
- [ ] Расчёт всех трёх типов формул (min_plus_per, fixed, expression)
- [ ] Отображение статистики на dashboard
- [ ] Фильтры на всех страницах
- [ ] Экспорт отчётов
- [ ] Telegram bot команды (если реализован)
- [ ] Опрос посещаемости через Telegram (если реализован)
- [ ] Security: SQL injection, XSS, CSRF

---

## 💾 База данных

**Бэкап**:
```bash
mysqldump -u cw95865_admin -p cw95865_admin > backup_$(date +%Y%m%d).sql
```

**Применение миграций**:
```bash
# Применить все миграции в правильном порядке
mysql -u cw95865_admin -p cw95865_admin < zarplata/migrations/add_room_to_lessons.sql
mysql -u cw95865_admin -p cw95865_admin < zarplata/migrations/add_tier_grades_students.sql
mysql -u cw95865_admin -p cw95865_admin < zarplata/migrations/add_formula_to_teachers.sql
```

---

## 📞 Контакты и поддержка

**Для вопросов**:
- Архитектура: `CLAUDE.md`
- База данных: `zarplata/database.sql`
- API: примеры в `zarplata/api/schedule.php`
- Документация: `zarplata/README.md` (Russian)

**Дефолтные креды** (⚠️ ИЗМЕНИТЬ В ПРОДАКШЕНЕ!):
- Username: `admin`
- Password: `admin123`

---

## 🎉 Критерий готовности

**Проект можно считать завершённым когда**:

✅ **Базовый функционал** (MVP):
- [x] Создание расписания
- [x] Управление преподавателями
- [x] Управление формулами
- [ ] Проведение уроков (смена статуса, фактические ученики)
- [ ] Автоматический расчёт зарплаты
- [ ] История выплат
- [ ] Approve и Mark as Paid

✅ **Расширенный функционал**:
- [ ] Отчёты (преподаватель, период)
- [ ] Экспорт в PDF/CSV
- [ ] Telegram bot (опционально)

✅ **Полировка**:
- [ ] Все страницы работают без ошибок
- [ ] Фильтры и поиск везде
- [ ] Audit log для всех действий
- [ ] Security audit пройден
- [ ] Документация актуальная

---

**Удачи в разработке! 🚀**
