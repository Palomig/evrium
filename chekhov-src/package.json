{
  "name": "chekhov-tutor-site",
  "version": "1.0.0",
  "description": "Сайт репетитора в Чехове на эвриум.рф",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "_clean_assets": "rm -rf ../_assets",
    "_rsync": "rsync -a --exclude='.git' --exclude='chekhov-src' --exclude='mcp-servers' --exclude='android-app' --exclude='docs' --exclude='crm' --exclude='zarplata' --exclude='oge-zadachi' dist/ ../",
    "deploy": "astro build && npm run _clean_assets && npm run _rsync"
  },
  "license": "ISC",
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.3.6"
  }
}
