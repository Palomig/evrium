{
  "name": "chekhov-tutor-site",
  "version": "1.0.0",
  "description": "Сайт репетитора в Чехове на эвриум.рф",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy": "astro build && rsync -a --exclude='.git' --exclude='chekhov-src' --exclude='mcp-servers' --exclude='android-app' --exclude='docs' --exclude='crm' --exclude='zarplata' --exclude='oge-zadachi' dist/ ../"
  },
  "license": "ISC",
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.3.6"
  }
}
