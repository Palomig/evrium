# 🚀 Инструкция по развертыванию на Timeweb

## Шаг 1: Сборка проекта

Выполните команду в корне проекта:

```bash
npm run build
```

Это создаст папку `dist` с готовыми файлами.

## Шаг 2: Подготовка файлов

После сборки у вас будет:
```
dist/
├── index.html
├── assets/
│   ├── index-XXXXX.js
│   ├── index-XXXXX.css
│   └── другие файлы
└── vite.svg
```

## Шаг 3: Загрузка на Timeweb

### Через Файловый менеджер:

1. Войдите в панель управления Timeweb
2. Откройте **Файловый менеджер**
3. Перейдите в папку `public_html` (или `www`)
4. **ВАЖНО:** Загрузите **содержимое папки dist**, а не саму папку:
   - Откройте папку `dist` на вашем компьютере
   - Выделите ВСЕ файлы внутри (index.html, assets/, vite.svg и т.д.)
   - Загрузите их в `public_html`

### Через FTP (FileZilla):

1. Подключитесь к хостингу по FTP
   - Хост: ваш домен или IP
   - Логин: из панели Timeweb
   - Пароль: из панели Timeweb
   - Порт: 21

2. Перейдите в папку `/public_html`
3. Загрузите все файлы из папки `dist`

## Шаг 4: Настройка .htaccess

Файл `.htaccess` уже создан в корне проекта. Загрузите его:

1. Скопируйте файл `.htaccess` из корня проекта
2. Загрузите в папку `public_html` на хостинге
3. **ВАЖНО:** Файл должен называться именно `.htaccess` (с точкой в начале)

## Шаг 5: Проверка

Откройте ваш домен в браузере. Сайт должен работать!

## 📂 Структура на хостинге должна быть:

```
public_html/
├── .htaccess          ← важен для React Router
├── index.html
├── assets/
│   ├── index-XXXXX.js
│   ├── index-XXXXX.css
│   └── ...
└── vite.svg
```

## ⚠️ Что НЕ загружать на хостинг:

- ❌ `node_modules/`
- ❌ `src/`
- ❌ `package.json`
- ❌ `package-lock.json`
- ❌ `vite.config.js`
- ❌ любые конфигурационные файлы

✅ Загружайте ТОЛЬКО содержимое папки `dist` + `.htaccess`

## 🔄 Обновление сайта

При внесении изменений:

1. Внесите изменения в код
2. Выполните `npm run build`
3. Загрузите новые файлы из `dist` на хостинг (с заменой старых)

## 🐛 Возможные проблемы

### Проблема: Страницы не работают при прямом переходе

**Решение:** Проверьте, что файл `.htaccess` загружен и mod_rewrite включен на хостинге.

### Проблема: Стили не применяются

**Решение:** Очистите кэш браузера (Ctrl+Shift+R или Cmd+Shift+R)

### Проблема: Ошибка 500

**Решение:** Проверьте права доступа к `.htaccess` (должны быть 644)

### Проблема: Интерактивные графики не работают

**Решение:** Проверьте, что библиотеки JSXGraph и MathJax загружаются (они подключены через CDN в index.html)

## 📞 Поддержка Timeweb

Если возникли проблемы с хостингом:
- Техподдержка: https://timeweb.com/ru/help/
- Телефон: 8 (800) 700-06-08
- Тикеты в личном кабинете

## ✨ Оптимизация (опционально)

Для ускорения загрузки сайта:

1. В личном кабинете Timeweb включите **CloudFlare** (если доступно)
2. Проверьте, что **mod_deflate** и **mod_expires** включены (они в .htaccess)
3. Включите **Gzip сжатие** в панели хостинга

---

**Готово!** Ваш сайт должен работать на `http://ваш-домен.ru`
